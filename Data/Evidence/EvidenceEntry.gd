# each individual evidence piece that goes into the court record
extends Button

# var evidence_icon: Texture setget _set_icon
# var evidence_name: String setget _set_name

# onready var icon_rect = $HBoxContainer/VBoxContainer/bg/Icon
# onready var name_label = $HBoxContainer/VBoxContainer/name

# func _set_icon(tex: Texture):
# 	icon_rect.set_texture(tex)

# func _set_name(string: String):
# 	name_label.set_text(string)
